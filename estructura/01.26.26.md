# Declaración IF

Es una expresesión booleana que tiene un condición
Si tiene una sola expresión no es necesario usar llaves

## Else if

Cuando tenemos mas condiciones
"if(bool-1) statement-1"
else if(bool-2) statement-2
else  statement-3"
